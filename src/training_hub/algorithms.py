def osf_continual_learning(model, dataset, ckpt_save_dir):
    """
    Continual Learning via Orthogonal Subspace Fine-tuning (OSF)

    Args:
        model: The model to train
        dataset: The dataset to train on
        ckpt_save_dir: The directory to save checkpoints in
    Returns:
    """
    print("OSF Continual Learning")

